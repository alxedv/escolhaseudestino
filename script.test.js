const { it, expect, test } = require('@jest/globals');
const { get } = require('http');
const { describe } = require('yargs');
// const { getAPI } = require('./script');

    test('Teste', () => {
        expect(1).toBe(1);
    });

